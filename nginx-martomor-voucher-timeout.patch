# Nginx config patch for martomor.xyz - increase timeout for voucher generation
# Apply this to /etc/nginx/sites-available/martomor.xyz on matmor-server-01
#
# Find the location block that proxies to the billing backend (port 5678)
# and add these directives inside it:
#
#   proxy_connect_timeout 120s;
#   proxy_send_timeout 180s;
#   proxy_read_timeout 180s;
#
# Example - if you have something like:
#
#   location /api/v1/ {
#       proxy_pass http://127.0.0.1:5678;
#       ...
#   }
#
# Add the timeout lines:
#
#   location /api/v1/ {
#       proxy_pass http://127.0.0.1:5678;
#       proxy_connect_timeout 120s;
#       proxy_send_timeout 180s;
#       proxy_read_timeout 180s;
#       proxy_set_header Host $host;
#       proxy_set_header X-Real-IP $remote_addr;
#       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#       proxy_set_header X-Forwarded-Proto $scheme;
#   }
